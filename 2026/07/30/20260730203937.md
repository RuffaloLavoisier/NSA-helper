# Commit: a14dad67f369affeffc1cd4d584b6b717cd9c3f5
## Message: Merge remote-tracking branch 'origin/GP-0-dragonmacher-test-fixes-7-28-26'
## Diff:
```
diff --git a/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorAlignmentTest.java b/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorAlignmentTest.java
index 4aa61116a8c..0817605f760 100644
--- a/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorAlignmentTest.java
+++ b/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorAlignmentTest.java
@@ -60,7 +60,8 @@ public void testNonPackedStructure() {
 				(pAction instanceof HexNumbersAction) ||
 				(pAction instanceof InsertUndefinedAction) ||
 				(pAction instanceof AddBitFieldAction) || (pAction instanceof ApplyAction) ||
-				pAction instanceof NextPrevDefinedComponentAction) {
+				pAction instanceof NextPrevDefinedComponentAction ||
+				pAction instanceof JumpToOffsetAction) {
 				checkEnablement(pAction, true);
 			}
 			else {
@@ -148,7 +149,8 @@ public void testEnablementDefaultAlignedStructure() throws Exception {
 				(pAction instanceof CreateInternalStructureAction) ||
 				(pAction instanceof ShowComponentPathAction) || (pAction instanceof ApplyAction) ||
 				(pAction instanceof FindReferencesToStructureFieldAction) ||
-				pAction instanceof NextPrevDefinedComponentAction) {
+				pAction instanceof NextPrevDefinedComponentAction ||
+				pAction instanceof JumpToOffsetAction) {
 				checkEnablement(pAction, true);
 			}
 			else {
diff --git a/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorLockedEnablementTest.java b/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorLockedEnablementTest.java
index 5ed39a25d7f..a3c0f78dc99 100644
--- a/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorLockedEnablementTest.java
+++ b/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorLockedEnablementTest.java
@@ -68,7 +68,8 @@ public void testEmptyStructureEditorState() {
 			if ((action instanceof EditFieldAction) || (action instanceof AddBitFieldAction) ||
 				(action instanceof InsertUndefinedAction) || (action instanceof PointerAction) ||
 				(action instanceof HexNumbersAction) || (action instanceof ApplyAction) ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else if (action instanceof FavoritesAction) {
@@ -120,7 +121,8 @@ public void testNonEmptyStructureEditorState() {
 				(action instanceof InsertUndefinedAction) || (action instanceof PointerAction) ||
 				(action instanceof HexNumbersAction) ||
 				(action instanceof ShowDataTypeInTreeAction) ||
-				(action instanceof NextPrevDefinedComponentAction)) {
+				(action instanceof NextPrevDefinedComponentAction) ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else if (action instanceof FavoritesAction) {
@@ -172,7 +174,8 @@ else if ((action instanceof CycleGroupAction) ||
 				(action instanceof CreateInternalStructureAction) ||
 				(action instanceof ShowDataTypeInTreeAction) ||
 				action instanceof FindReferencesToStructureFieldAction ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else {
@@ -204,7 +207,8 @@ else if ((action instanceof CycleGroupAction) ||
 				(action instanceof CreateInternalStructureAction) ||
 				(action instanceof ShowDataTypeInTreeAction) ||
 				action instanceof FindReferencesToStructureFieldAction ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else {
@@ -236,7 +240,8 @@ else if ((action instanceof CycleGroupAction) ||
 				(action instanceof CreateInternalStructureAction) ||
 				(action instanceof ShowDataTypeInTreeAction) ||
 				action instanceof FindReferencesToStructureFieldAction ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else {
@@ -266,7 +271,8 @@ else if ((action instanceof CycleGroupAction) ||
 				(action instanceof PointerAction) || (action instanceof HexNumbersAction) ||
 				(action instanceof CreateInternalStructureAction) ||
 				(action instanceof ShowDataTypeInTreeAction) ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else {
@@ -292,7 +298,8 @@ public void testNonContiguousSelectionEnablement() {
 			else if ((action instanceof CycleGroupAction) || (action instanceof ClearAction) ||
 				(action instanceof DeleteAction) || (action instanceof HexNumbersAction) ||
 				(action instanceof ShowDataTypeInTreeAction) ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else {
diff --git a/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorUnlockedEnablementTest.java b/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorUnlockedEnablementTest.java
index 9d94316e800..de89b0fc61e 100644
--- a/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorUnlockedEnablementTest.java
+++ b/Ghidra/Features/Base/src/test.slow/java/ghidra/app/plugin/core/compositeeditor/StructureEditorUnlockedEnablementTest.java
@@ -50,7 +50,8 @@ public void testEmptyStructureEditorState() {
 				(action instanceof EditFieldAction) || (action instanceof InsertUndefinedAction) ||
 				(action instanceof AddBitFieldAction) || (action instanceof PointerAction) ||
 				(action instanceof HexNumbersAction) || (action instanceof ApplyAction) ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else {
@@ -86,7 +87,8 @@ public void testNonEmptyStructureEditorState() throws Exception {
 				(action instanceof AddBitFieldAction) || (action instanceof PointerAction) ||
 				(action instanceof HexNumbersAction) ||
 				(action instanceof ShowDataTypeInTreeAction) ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else {
@@ -122,7 +124,8 @@ public void testFirstComponentSelectedEnablement() {
 				(action instanceof CreateInternalStructureAction) ||
 				(action instanceof ShowDataTypeInTreeAction ||
 					action instanceof FindReferencesToStructureFieldAction) ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else if (action instanceof FavoritesAction) {
@@ -164,7 +167,8 @@ public void testCentralComponentSelectedEnablement() {
 				(action instanceof CreateInternalStructureAction) ||
 				(action instanceof ShowDataTypeInTreeAction ||
 					action instanceof FindReferencesToStructureFieldAction) ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else if (action instanceof FavoritesAction) {
@@ -207,7 +211,8 @@ public void testLastComponentSelectedEnablement() {
 				(action instanceof CreateInternalStructureAction) ||
 				(action instanceof ShowDataTypeInTreeAction ||
 					action instanceof FindReferencesToStructureFieldAction) ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else if (action instanceof FavoritesAction) {
@@ -248,7 +253,8 @@ public void testBlankLineSelectedEnablement() {
 				(action instanceof AddBitFieldAction) || (action instanceof PointerAction) ||
 				(action instanceof HexNumbersAction) ||
 				(action instanceof ShowDataTypeInTreeAction) ||
-				action instanceof NextPrevDefinedComponentAction) {
+				action instanceof NextPrevDefinedComponentAction ||
+				action instanceof JumpToOffsetAction) {
 				checkEnablement(action, true);
 			}
 			else {
```
-----------------------------------
