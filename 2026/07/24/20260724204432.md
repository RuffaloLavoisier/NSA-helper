# Commit: 64edf25baf41b96a9b3733bfe37fd8e66d3ae3b4
## Message: Merge remote-tracking branch 'origin/GP-7058_caheckman_IllegalOpIBlock'
(Closes #9282)
## Diff:
```
diff --git a/Ghidra/Features/Decompiler/src/decompile/cpp/condexe.cc b/Ghidra/Features/Decompiler/src/decompile/cpp/condexe.cc
index 97d7459eeb3..67a01dee3aa 100644
--- a/Ghidra/Features/Decompiler/src/decompile/cpp/condexe.cc
+++ b/Ghidra/Features/Decompiler/src/decompile/cpp/condexe.cc
@@ -244,13 +244,13 @@ Varnode *ConditionalExecution::resolveIblockRead(PcodeOp *op,int4 inbranch)
 {
   if (op->code() == CPUI_COPY) {
     Varnode *vn = op->getIn(0);
-    if (vn->isWritten()) {
-      PcodeOp *defOp = vn->getDef();
-      if (defOp->code() == CPUI_MULTIEQUAL && defOp->getParent() == iblock)
-	op = defOp;
-    }
-    else
+    if (!vn->isWritten())
       return vn;
+    PcodeOp *defOp = vn->getDef();
+    if (defOp->code() == CPUI_MULTIEQUAL && defOp->getParent() == iblock)
+      op = defOp;
+    else
+      return vn;	// We know defOp is not in iblock
   }
   OpCode opc = op->code();
   if (opc == CPUI_MULTIEQUAL)
```
-----------------------------------
