# Commit: c561a6f80210b008f7f8a19facf3ce65f06bb24a
## Message: GP-6349: LaunchSupport now can use application.settingsdir system
property in launch.properties
## Diff:
```
diff --git a/GhidraBuild/LaunchSupport/src/main/java/ghidra/launch/AppConfig.java b/GhidraBuild/LaunchSupport/src/main/java/ghidra/launch/AppConfig.java
index 769dc374479..fe3a999558e 100644
--- a/GhidraBuild/LaunchSupport/src/main/java/ghidra/launch/AppConfig.java
+++ b/GhidraBuild/LaunchSupport/src/main/java/ghidra/launch/AppConfig.java
@@ -402,6 +402,7 @@ private File getSaveFile(File installDir, String saveFileName) throws FileNotFou
 			userSettingsDirName += "_location_" + dirName;
 		}
 		
+
 		// Ensure there is a user home directory (there definitely should be)
 		String userHomeDirPath = System.getProperty("user.home");
 		if (userHomeDirPath == null || userHomeDirPath.isEmpty()) {
@@ -420,6 +421,20 @@ private File getSaveFile(File installDir, String saveFileName) throws FileNotFou
 			return new File(userSettingsDir, saveFileName);
 		}
 
+		// Handle application.settingsdir system property
+		for (String arg : launchProperties.getVmArgList()) {
+			if (arg.startsWith("-Dapplication.settingsdir")) {
+				String[] parts = arg.split("=", 2);
+				if (parts.length == 2) {
+					String path = parts[1].trim();
+					if (!path.isEmpty()) {
+						userSettingsDir = new File(path, appName + "/" + userSettingsDirName);
+						return new File(userSettingsDir, saveFileName);
+					}
+				}
+			}
+		}
+
 		// Look for XDG environment variable
 		String xdgConfigHomeDirStr = System.getenv("XDG_CONFIG_HOME");
 		if (xdgConfigHomeDirStr != null && !xdgConfigHomeDirStr.isEmpty()) {
```
-----------------------------------
