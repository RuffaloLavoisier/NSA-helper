# Commit: 93973532da4a7be3869f21caf4d50d9ce6d87549
## Message: Merge remote-tracking branch 'origin/GP-0-dragonmacher-test-fixes-5-12-26'
## Diff:
```
diff --git a/Ghidra/Features/Base/src/main/java/ghidra/app/plugin/core/function/editor/FunctionEditorModel.java b/Ghidra/Features/Base/src/main/java/ghidra/app/plugin/core/function/editor/FunctionEditorModel.java
index 97426a21848..f683704386b 100644
--- a/Ghidra/Features/Base/src/main/java/ghidra/app/plugin/core/function/editor/FunctionEditorModel.java
+++ b/Ghidra/Features/Base/src/main/java/ghidra/app/plugin/core/function/editor/FunctionEditorModel.java
@@ -798,9 +798,10 @@ private boolean applyFunctionData(boolean commitFullParamDetails) {
 				function.setName(name, SourceType.USER_DEFINED);
 			}
 
-			Namespace namespace = functionData.getNamespace();
-			if (!namespace.equals(function.getParentNamespace())) {
-				function.setParentNamespace(namespace);
+			Namespace newNamespace = functionData.getNamespace();
+			Namespace currentNamespace = function.getParentNamespace();
+			if (!Objects.equals(newNamespace, currentNamespace)) {
+				function.setParentNamespace(newNamespace);
 			}
 
 			boolean isInline = functionData.isInline();
@@ -1057,7 +1058,7 @@ void parseSignatureFieldText() throws ParseException, CancelledException {
 	private Namespace createNamespace(SymbolPath path) throws ParseException {
 
 		if (path == null) {
-			return null;
+			return program.getGlobalNamespace();
 		}
 
 		String nsText = path.getPath();
```
-----------------------------------
