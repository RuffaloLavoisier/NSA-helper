# Commit: f0a867b084bd83cb9c9f65743aa89d9d82ad6a8c
## Message: GP-6385: Setting failOnNoDiscoveredTests for Gradle tests (in the right
place this time)
## Diff:
```
diff --git a/gradle/javaTestProject.gradle b/gradle/javaTestProject.gradle
index bfe455092f4..e2991aa2c64 100644
--- a/gradle/javaTestProject.gradle
+++ b/gradle/javaTestProject.gradle
@@ -132,6 +132,10 @@ def initTestJVM(Task task, String rootDirName) {
 	// If false, testing will halt when an error is found.
 	task.ignoreFailures = true
 	
+	// Whether or failure should occur if tests sources are present, but no tests are discovered
+	// during test execution. New for Gradle 9 (behavior is equivalent to false on Gradle 8).
+	task.failOnNoDiscoveredTests = false
+	
 	// If false, then tests are re-run every time, even if no code has changed.
 	task.outputs.upToDateWhen {false}
 	
diff --git a/gradle/root/test.gradle b/gradle/root/test.gradle
index 143b1092b33..6eb7ce0b2d0 100644
--- a/gradle/root/test.gradle
+++ b/gradle/root/test.gradle
@@ -300,10 +300,6 @@ def initTestJVM(Task task, String rootDirName) {
 	// If false, testing will halt when an error is found.
 	task.ignoreFailures true
 	
-	// Whether or failure should occur if tests sources are present, but no tests are discovered
-	// during test execution. New for Gradle 9 (behavior is equivalent to false on Gradle 8).
-	task.failOnNoDiscoveredTests = false
-	
 	// If false, then tests are re-run every time, even if no code has changed.
 	task.outputs.upToDateWhen {false}
 	
```
-----------------------------------
