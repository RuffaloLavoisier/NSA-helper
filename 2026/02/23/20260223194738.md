# Commit: b4fb21768151812f2b5195de3cfee8f044fbe021
## Message: Merge remote-tracking branch 'origin/GP-6454_dev747368_gccexceptionanalyzer_taskmonitor_update'
## Diff:
```
diff --git a/Ghidra/Features/Base/src/main/java/ghidra/app/plugin/exceptionhandlers/gcc/GccExceptionAnalyzer.java b/Ghidra/Features/Base/src/main/java/ghidra/app/plugin/exceptionhandlers/gcc/GccExceptionAnalyzer.java
index 51658f0ce56..fabac226e03 100644
--- a/Ghidra/Features/Base/src/main/java/ghidra/app/plugin/exceptionhandlers/gcc/GccExceptionAnalyzer.java
+++ b/Ghidra/Features/Base/src/main/java/ghidra/app/plugin/exceptionhandlers/gcc/GccExceptionAnalyzer.java
@@ -175,18 +175,15 @@ private void handleStandardSections(Program program, TaskMonitor monitor, Messag
 			List<RegionDescriptor> regions = ehframeSection.analyze(fdeTableCount);
 
 			AddressSet ehProtected = new AddressSet();
-
+			monitor.initialize(getCallSiteRecordCount(regions), "Marking up Call Site records");
 			for (RegionDescriptor region : regions) {
-
-				monitor.checkCancelled();
 				ehProtected.add(region.getRange());
 
 				LSDACallSiteTable callSiteTable = region.getCallSiteTable();
 				if (callSiteTable != null) {
-
 					// Process this table's call site records.
 					for (LSDACallSiteRecord cs : callSiteTable.getCallSiteRecords()) {
-						monitor.checkCancelled();
+						monitor.increment();
 						processCallSiteRecord(program, ehProtected, region, cs);
 					}
 				}
@@ -199,6 +196,17 @@ private void handleStandardSections(Program program, TaskMonitor monitor, Messag
 		}
 	}
 
+	private int getCallSiteRecordCount(List<RegionDescriptor> regions) {
+		int total = 0;
+		for (RegionDescriptor region : regions) {
+			LSDACallSiteTable callSiteTable = region.getCallSiteTable();
+			if (callSiteTable != null) {
+				total += callSiteTable.getCallSiteRecords().size();
+			}
+		}
+		return total;
+	}
+
 	private void processCallSiteRecord(Program program, AddressSet ehProtected,
 			RegionDescriptor region, LSDACallSiteRecord cs) {
 		AddressRange callSite = cs.getCallSite();
```
-----------------------------------
