# Commit: f2af386845a2617490cd96044356335773124a5b
## Message: GP-0 Rtti fix for failing tests - missing null check in recent update.
## Diff:
```
diff --git a/Ghidra/Features/Decompiler/ghidra_scripts/classrecovery/RTTIWindowsClassRecoverer.java b/Ghidra/Features/Decompiler/ghidra_scripts/classrecovery/RTTIWindowsClassRecoverer.java
index f8ae30e3cb5..31e5776379f 100644
--- a/Ghidra/Features/Decompiler/ghidra_scripts/classrecovery/RTTIWindowsClassRecoverer.java
+++ b/Ghidra/Features/Decompiler/ghidra_scripts/classrecovery/RTTIWindowsClassRecoverer.java
@@ -1569,6 +1569,12 @@ private boolean isInvalidVbtable(Address address) {
 		// it needs offset values and most are either large negatives (FFFFF....) or small offsets
 		// both of which are not valid addresses in a normal PE binary
 		Address referencedAddress = extendedFlatAPI.getPointer(address);
+
+		// if null then not an address so possibly valid
+		if (referencedAddress == null) {
+			return false;
+		}
+		// if is program memory location then invalid
 		if (program.getMemory().contains(referencedAddress)) {
 			return true;
 		}
```
-----------------------------------
