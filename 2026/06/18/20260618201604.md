# Commit: b1f9b648bbc45f5f6606917fb3a72ce56b4de94c
## Message: GP-1 Corrected regression to SymbolDB refresh from GP-6634 change
## Diff:
```
diff --git a/Ghidra/Framework/SoftwareModeling/src/main/java/ghidra/program/database/symbol/SymbolDB.java b/Ghidra/Framework/SoftwareModeling/src/main/java/ghidra/program/database/symbol/SymbolDB.java
index 6aa4db52401..54bde44ee78 100644
--- a/Ghidra/Framework/SoftwareModeling/src/main/java/ghidra/program/database/symbol/SymbolDB.java
+++ b/Ghidra/Framework/SoftwareModeling/src/main/java/ghidra/program/database/symbol/SymbolDB.java
@@ -94,7 +94,12 @@ protected boolean refresh(DBRecord rec) {
 			record = rec;
 			Address newAddress = symbolMgr.getAddressMap()
 					.decodeAddress(rec.getLongValue(SymbolDatabaseAdapter.SYMBOL_ADDR_COL));
-			return address.equals(newAddress);
+			boolean isAddrSame = address.equals(newAddress);
+			address = newAddress;
+			// NOTE: If isAddrSame is not true, the symbol address changed which implies this 
+			// symbol was deleted and the key re-used.  It is important to note this instance
+			// is not the same symbol as the previous record state and should not be used.  
+			return isAddrSame;
 		}
 		return false;
 	}
```
-----------------------------------
