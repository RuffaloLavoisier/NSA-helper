# Commit: 0394d6e1a1034922245540809a77dbe41d3b4dbe
## Message: Merge remote-tracking branch 'origin/GP-6939-dragonmacher-decomp-repaint-issue'
## Diff:
```
diff --git a/Ghidra/Features/Decompiler/src/main/java/ghidra/app/plugin/core/decompile/DecompilerProvider.java b/Ghidra/Features/Decompiler/src/main/java/ghidra/app/plugin/core/decompile/DecompilerProvider.java
index dd74b391f02..49a47b216a6 100644
--- a/Ghidra/Features/Decompiler/src/main/java/ghidra/app/plugin/core/decompile/DecompilerProvider.java
+++ b/Ghidra/Features/Decompiler/src/main/java/ghidra/app/plugin/core/decompile/DecompilerProvider.java
@@ -350,11 +350,11 @@ private void doRefresh(boolean optionsChanged) {
 
 		if (currentLocation != null) {
 			if (lockDisplay) {
-				overlayPainter.setMessage(getOverlayRefreshMessage());
+				setOverlayMessage(getOverlayRefreshMessage());
 			}
 			else {
 				controller.refreshDisplay(program, currentLocation, null);
-				overlayPainter.setMessage("");
+				setOverlayMessage("");
 			}
 		}
 	}
@@ -370,10 +370,15 @@ private String getOverlayRefreshMessage() {
 
 	private void updateOverlayMessage() {
 		if (overlayPainter.isActive()) {
-			overlayPainter.setMessage(getOverlayRefreshMessage());
+			setOverlayMessage(getOverlayRefreshMessage());
 		}
 	}
 
+	private void setOverlayMessage(String s) {
+		overlayPainter.setMessage(s);
+		decorationPanel.repaint();
+	}
+
 	private void refreshToggleButtons() {
 		displayUnreachableCodeToggle.setSelected(!decompilerOptions.isEliminateUnreachable());
 		respectReadOnlyFlags.setSelected(!decompilerOptions.isRespectReadOnly());
@@ -528,7 +533,7 @@ void setLocation(ProgramLocation loc, ViewerPosition viewerPosition) {
 	 */
 	void refresh() {
 		controller.refreshDisplay(program, currentLocation, null);
-		overlayPainter.setMessage("");
+		setOverlayMessage("");
 	}
 
 	/**
```
-----------------------------------
