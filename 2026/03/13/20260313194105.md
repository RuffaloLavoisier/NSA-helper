# Commit: 39612c559f749f0674b12eb3f6b17c37531e7f1a
## Message: GP-4045: Excluding *.sla.lock files from build
## Diff:
```
diff --git a/gradle/distributableGhidraModule.gradle b/gradle/distributableGhidraModule.gradle
index 5a7ab5ffef3..a1b382db3e5 100644
--- a/gradle/distributableGhidraModule.gradle
+++ b/gradle/distributableGhidraModule.gradle
@@ -40,7 +40,8 @@ rootProject.assembleDistribution {
 
 	from ("${p.projectDir}/data") {
 		into { "${zipPath}/data" }
-		exclude 'build.xml' // associated with language modules (dev use only)
+		exclude 'build.xml'   // associated with language modules (dev use only)
+		exclude '**/*.sla.lock' // associated with language modules
 	}
 	
 	from ("${BIN_REPO}/${getGhidraRelativePath(p)}/data") {
diff --git a/gradle/processorProject.gradle b/gradle/processorProject.gradle
index dab7a515606..578e17b11d8 100644
--- a/gradle/processorProject.gradle
+++ b/gradle/processorProject.gradle
@@ -202,7 +202,7 @@ task cleanSleigh  {
 	group = rootProject.GHIDRA_GROUP
 	description = "Removes all the compile sleigh language files (*.sla).   [gradle/processorProject.gradle]\n"
  	doLast {
-		def deleteTree = fileTree(dir: "data/languages", include: "*.sla")
+		def deleteTree = fileTree(dir: "data/languages", include: ["*.sla", "*.sla.lock"])
 		deleteTree.each { File file ->
 			delete file
 		}
diff --git a/gradle/support/ip.gradle b/gradle/support/ip.gradle
index 8244aa27866..ff5a990ea22 100644
--- a/gradle/support/ip.gradle
+++ b/gradle/support/ip.gradle
@@ -147,6 +147,7 @@ def Map<String, List<String>> getIpForModule(Project p) {
 		exclude "buildNatives.gradle"		
 		exclude "**/Misc/Tips.htm"
 		exclude "**/*.sla"
+		exclude "**/*.sla.lock"
 		exclude "**/.gradle/**"
 		exclude "**/.settings/**"
 		exclude "**/data/build.xml" // language build file (generated for dev only)
```
-----------------------------------
