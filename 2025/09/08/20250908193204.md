# Commit: c65c4c777f5946f9752a372619908e2c4a7770b1
## Message: Fix for centering components on items inside of scroll panes
## Diff:
```
diff --git a/Ghidra/Framework/Generic/src/main/java/generic/util/WindowUtilities.java b/Ghidra/Framework/Generic/src/main/java/generic/util/WindowUtilities.java
index 21c4a1aa6f6..d03096c8d70 100644
--- a/Ghidra/Framework/Generic/src/main/java/generic/util/WindowUtilities.java
+++ b/Ghidra/Framework/Generic/src/main/java/generic/util/WindowUtilities.java
@@ -21,6 +21,7 @@
 import java.util.List;
 import java.util.Objects;
 
+import javax.swing.JViewport;
 import javax.swing.SwingUtilities;
 
 import ghidra.util.Swing;
@@ -225,6 +226,14 @@ private static Point center(Rectangle area, Dimension d) {
 	 * the size of the given <code>child</code>.
 	 */
 	public static Point centerOnComponent(Component parent, Component child) {
+
+		// Clients inside of scroll panes can be much larger than what is visible.  We only wish to
+		// use the visible size when calculating the center position.
+		Container grandParent = parent.getParent();
+		if (grandParent instanceof JViewport) {
+			parent = grandParent.getParent();
+		}
+
 		Dimension parentSize = parent.getSize();
 		Dimension childSize = child.getSize();
 		int x = (parentSize.width >> 1) - (childSize.width >> 1);
```
-----------------------------------
